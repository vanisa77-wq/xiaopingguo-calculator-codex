# 小蘋果計算機 使用說明書

## 一、基本計算

1. 依序按數字與運算符號。
2. 按 `=` 顯示答案。
3. 計算順序為先乘除、後加減。
4. 每次完成的算式與答案會自動保留在上方紙帶。

範例：

`2 + 3 × 4 = 14`

## 二、數字與運算按鍵

- `00`：輸入兩個零。
- `0`：輸入零。
- `.`：輸入小數點。
- `+`：加法。
- `−`：減法。
- `×`：乘法。
- `÷`：除法。
- `=`：計算並顯示答案。

## 三、括號

- 按 `( )` 可智慧加入左括號或右括號。
- 計算機會依目前算式判斷應加入哪一種括號。
- 如果算式缺少開頭左括號，計算時會自動補齊。

範例：

`(2 + 3) × 4 = 20`

## 四、刪除與清除

### 刪除鍵

- 單按 `⌫`：刪除最後一個字。
- 長按 `⌫`：連續刪除。

### AC

- 清除目前輸入的算式。
- 不會刪除上方紙帶紀錄。
- 會結束目前的稅金或發票計算狀態。

## 五、紙帶紀錄

- 每題按 `=` 後，算式與答案會自動加入紙帶。
- 紙帶會自動保存在目前裝置，關閉後再次開啟仍可查看。
- 最多保存最近 100 筆計算紀錄。
- 紙帶紀錄靠左排列；長算式會自動換行並完整顯示。
- 六位數以上的連續加減，也會完整保留算式與答案。
- 可在顯示區向上滑動，查看先前所有紀錄。
- 一般計算不會顯示「稅金」或「發票」標籤。
- 只有按「5%稅」或「發票」時，才會出現相應標籤。

### 選擇歷史算式

點一下紙帶中的任一筆算式，該筆紀錄會顯示選取底色，接著可以使用「複製」、「截圖」或「＋註記」。

## 六、算式名稱與數字標籤

「＋註記」可以在計算前、計算途中或計算完成後使用。

按下「＋註記」後，鍵盤上方會出現緊湊的小欄位，每一列最多排列三組註記。「算式」及各個數值可以一次填完，最後只要按一次「儲存」。

### 算式名稱

算式名稱用來說明整筆計算的用途，例如：

`7月份房租結算`

### 數字前方的小標籤

每個數字都可以設定自己的標籤，標籤會顯示在數字前方：

`每月房租|34000×月份|12=應付總額|408,000`

截圖與複製文字使用窄版的 `|` 分隔註記和數值，不保留多餘空格；換行時會把註記和數字一起移到下一行。

### 計算前或計算途中加入

1. 輸入數字，或先按「＋註記」。
2. 在「算式」或各個數值旁輸入幾個字。
3. 可以同時填寫多個註記。
4. 最後按一次「儲存」後繼續計算。

如果算式目前停在運算符號後方，註記畫面會提供「下一個數字」，可以先替即將輸入的數字設定標籤。

### 計算完成後補上

1. 點選紙帶中的歷史算式。
2. 按「＋註記」。
3. 新增或修改算式名稱、數字標籤及答案標籤。
4. 按「儲存」。

修改註記不會重新計算，也不會改變答案。

## 七、ANS 三種答案

三顆 ANS 都使用上一題的答案開始新算式。

### ANS 0

四捨五入至整數。

- `26666.44 → 26666`
- `26666.57 → 26667`

### ANS .00

四捨五入至小數第 2 位。

- `26666.576 → 26666.58`

### ANS ↑

無條件進位至整數。

- `26666.01 → 26667`

按任一 ANS 後仍會保留「複製」鍵，可複製目前顯示的版本。

## 八、設𝒳 記憶數字

`設𝒳` 可把剛算完的答案存成 𝒳，之後在新算式中直接使用，不需要重打很多位數。

### 設定 𝒳

1. 先完成一題計算。
2. 按 `設𝒳`。
3. 計算機會記住目前答案為 𝒳。

範例：

`1 + 456788 = 456789`

按 `設𝒳` 後：

`𝒳 = 456789`

### 使用 𝒳

設定完成後，在下一題算式中按 `設𝒳`，就會插入 𝒳。

範例：

`123 × 𝒳 = 56,185,047`

也就是：

`123 × 456789 = 56,185,047`

若尚未設定 𝒳，按 `設𝒳` 會提示先算出答案再設定。

## 九、複製與截圖

1. 按 `=` 完成計算。
2. 顯示區會出現「複製」鍵。
3. 按「複製」即可將算式名稱、數字標籤、完整算式與答案存入手機剪貼簿。
4. 可到 LINE、備忘錄或其他 App 貼上。

按「複製」也會把該答案記錄為可供稅金或發票功能使用的本金。

按「截圖」後可以選擇：

- 目前算式：包含尚未完成的算式，並標示為「計算中」。
- 選擇的歷史算式：只截取剛才點選的紀錄。
- 全部計算紀錄：把紙帶中的所有算式整理成一張長圖。

截圖會保留算式名稱、數字標籤、答案標籤及製作時間，並開啟分享選單；如果裝置不支援分享選單，會直接下載圖片。

同一題有兩個以上的數字註記時，截圖會自動改成明細條列：每筆註記與金額各占一行，最下方顯示合計金額。分享圖片時只傳送圖片，不會再另外產生一則重複的文字訊息。

## 十、5% 稅金

稅金功能支援兩種本金來源。

### 方法一：直接輸入本金

1. 輸入本金，例如 `58961`。
2. 直接按「5%稅」。
3. 紙帶與顯示區會列出：

`58,961 × 5% = 2,948`

### 方法二：使用複製的本金

1. 先完成一題計算。
2. 按「複製」記錄本金。
3. 按「5%稅」。

稅金依發票使用方式四捨五入至整數，不保留小數。

- `58,961 × 5% = 2,948.05 → 2,948`
- `58,970 × 5% = 2,948.5 → 2,949`

完成後會顯示「稅金」標籤與「複製」鍵。

## 十一、發票總額

發票總額為：

`本金 + 5%稅金`

### 方法一：直接輸入本金

1. 輸入本金，例如 `58961`。
2. 直接按「發票」。
3. 顯示：

`58,961 + 2,948 = 61,909`

### 方法二：先算稅金，再算發票

1. 輸入本金。
2. 按「5%稅」。
3. 再按「發票」。
4. 計算機會記住原本金，不會把稅金誤認為本金。

### 方法三：使用複製的本金

1. 完成一題計算。
2. 按「複製」。
3. 按「發票」。

完成後，紙帶及顯示區會出現「發票」標籤，並保留「複製」鍵。

## 十二、結束稅金或發票模式

- 完成稅金或發票計算後，直接按新的數字鍵即可開始一般計算。
- 新算式不會沿用「稅金」或「發票」標籤。
- 按 AC、刪除、括號或一般運算符號，也會結束上一筆特殊計算的顯示狀態。

## 十三、常見提示

- 「算式還沒完成」：最後一個字是運算符號或左括號，請先完成算式。
- 「請直接輸入金額，或先按複製」：尚未提供可計算稅金或發票的本金。

## 十四、2026-07-30 更新紀錄

### 註記功能

- 可在計算前、計算途中或計算完成後加入註記。
- 算式名稱與多個數字標籤可一次填寫，最後只需按一次「儲存」。
- 註記欄位採緊湊排列，一列最多顯示三組，並保留下方原有計算按鍵。
- 註記文字使用較清楚的深色字體，不加粗，標籤與數字之間保留適當間距。
- 手機註記欄位已設定為繁體中文文字輸入，並關閉英文自動大寫、拼字修正與自動校正。
- 網頁無法強制指定手機使用「倉頡」鍵盤；第一次仍需手動切換，之後手機通常會沿用最近使用的鍵盤。

### 紙帶與顯示版面

- 加大紙帶顯示區，按鍵寬度維持不變並降低按鍵高度。
- 紙帶頂端與底端保留完整顯示空間，避免第一行斷字或最後一筆紀錄被遮住。
- 長算式與較大數字會自動調整排列，避免文字和金額擠在一起。
- 註記與數字以 `註記|金額` 顯示；分隔線已調整至文字中央，並保留少量間距。

### 截圖與分享

- 可截取目前算式、已選取的歷史算式或全部計算紀錄。
- 同一題有兩個以上的數字註記時，截圖會自動改為由上往下的明細條列。
- 條列中的名稱與金額分欄排列，所有金額靠右對齊，合計金額與上方數字對齊。
- 算式名稱會比明細文字稍大，合計顯示在最下方。
- 分享時只傳送圖片，避免同時出現一份文字與一張圖片的重複內容。

### 聲音功能

- 中文數字語音與「大量硬幣倒入盒中」音效目前仍在試聽調整。
- 聲音尚未加入正式版計算機。

## 十五、2026-07-31 奶油金橘配色

已選用三套方案中的第 2 套「奶油金橘」：

- 計算機主體背景：奶油米色 `#F6E8D2`
- 顯示區：暖白色 `#FFFDF8`
- 所有數字鍵與小數點：淡奶油色 `#FAF4E8`
- `＋`、`−`、`×`、`÷`：陶土橘 `#D9876C`
- ANS、設𝒳、刪除、括號、稅率、發票、複製、截圖與註記：芥末金 `#D8AA43`
- AC：霧青綠 `#82AAA1`
- `=`：深藍灰 `#486B7B`

按鍵依用途固定使用同一顏色，兼具溫暖感與功能辨識度。

顯示螢幕同步採用自然平衡樣式：

- 一般歷史算式使用較細的深灰褐色字體。
- 答案使用較深、稍粗的字體，與算式形成清楚層次。
- 每筆紀錄之間使用陶土橘細分隔線。
- 選取中的紀錄使用淡霧青底色 `#C8DCD6`，左側搭配深青綠識別線 `#5F8C83`。
- 選取時的算式文字使用深灰 `#4A4D49`，標題與答案使用更深的 `#303531`，提高閱讀對比。
- 最下方目前算式維持一般字重，最終答案另外放大並加深。

截圖圖片同步使用「奶油金橘」色系：

- 圖片背景使用奶油米色 `#F6E8D2`，內容區使用暖白色 `#FFFDF8`。
- 標題使用深藍灰 `#486B7B`，分隔線使用陶土橘 `#D9876C`。
- 一般文字使用灰褐色，金額使用深色，提高分享圖片的閱讀清晰度。
- 已移除內容區外圍原有的粗框線，截圖版面更簡潔。
- 已移除截圖頂端的「小蘋果計算機」、日期時間與標題分隔線。
- 明細內容會自動向上排列，並依內容縮短圖片高度，不會在刪除標題後留下大片空白。

### 截圖版本：陶土雙層框

2026-07-31 已選用第 3 套截圖設計：

- 圖片背景：柔粉陶土色 `#F3DED3`
- 內容紙張：暖白色 `#FFF9F4`
- 主外框：陶土色 `#D9876C`
- 內框：淡粉色 `#E9C7B8`
- 算式名稱與合計標籤：深陶土褐 `#87503F`
- 一般項目文字：深灰褐 `#4B4541`
- 金額與合計金額：近黑褐 `#302E2B`
- 合計分隔線：芥末金 `#D8AA43`
- 標題、項目、金額與合計文字均比前一版放大，提高手機閱讀辨識度。
- 截圖寬度會依最長項目名稱與最寬金額自動調整，短內容不再保留大片右側空白；長文字與大金額仍會自動加寬，避免裁切。
- 標題、明細、分隔線與合計會整組垂直置中，使內框上、下留白較平均，避免內容過度貼近上方。

### 手機註記欄位

- 手機寬度下仍保留一列最多三組註記。
- 每組改為「數字名稱在上、輸入框在下」的等寬排列，避免第三組欄位超出手機右側邊界。
- 註記面板會限制在螢幕寬度內，取消與儲存按鍵不再被推到畫面之外。
- 極窄螢幕會自動改為每列兩組，確保欄位仍可正常輸入。
- 「先算一題才有 ANS」：尚未產生上一題答案。
- 「先算出答案，再按設𝒳」：尚未設定 𝒳。
- 除數不可為零。

## 十四、安裝到手機

### iPhone / iPad

1. 使用 Safari 開啟計算機網址。
2. 點「分享」。
3. 選擇「加入主畫面」。
4. 名稱輸入「小蘋果計算機」。
5. 點「加入」。

之後可直接從 iPhone 主畫面開啟使用。

如果圖示或功能沒有更新，請先刪除舊的主畫面圖示，再用 Safari 重新加入一次。

### Android 手機

1. 使用 Chrome 開啟計算機網址。
2. 點右上角 `⋮`。
3. 選擇「加到主畫面」或「安裝應用程式」。
4. 名稱輸入「小蘋果計算機」。
5. 點「新增」或「安裝」。

之後可直接從 Android 主畫面開啟使用。

## 十五、安裝到電腦

### Mac

1. 使用 Safari 開啟 GitHub Pages 正式網址：
   `https://vanisa77-wq.github.io/xiaopingguo-calculator-codex/`
2. 點上方選單「檔案」。
3. 選擇「加入 Dock」。
4. 名稱輸入「小蘋果計算機」。
5. 點「加入」。

完成後，計算機會出現在 Mac 的 Dock 與「應用程式」中。

若「加入 Dock」呈現反灰：

- 不可直接開啟外接磁碟或 Mac 裡的 `index.html`；網址列若以 `file://` 開頭，不能用此方式加入 Dock。
- 必須用 Safari 開啟以 `https://` 開頭的 GitHub Pages 正式網址。
- 此功能需要 macOS Sonoma 14 或更新版本。
- 更新計算機後，應先把新版 `index.html` 上傳 GitHub，再從正式網址加入 Dock。

### Windows

使用 Chrome：

1. 開啟計算機網址。
2. 點右上角 `⋮`。
3. 選擇「儲存及分享」或「更多工具」。
4. 選擇「建立捷徑」或「安裝頁面為應用程式」。
5. 勾選「以視窗開啟」。
6. 名稱輸入「小蘋果計算機」。
7. 點「建立」。

使用 Microsoft Edge：

1. 開啟計算機網址。
2. 點右上角 `…`。
3. 選擇「應用程式」。
4. 選擇「將此網站安裝為應用程式」。
5. 名稱輸入「小蘋果計算機」。
6. 點「安裝」。

## 十六、更新到 GitHub

如果只是在 Mac 本機修改，手機網址不會自動變成最新版。要讓手機、iPad、其他電腦看到最新版，需要把檔案上傳到 GitHub。

### 為什麼要存入 GitHub

1. GitHub 是放「手機正式網址」的地方。
2. iPhone、iPad、Android 和其他電腦打開的網址，是 GitHub Pages 產生的網站。
3. 如果只改 Mac 本機檔案，只有這台 Mac 看得到新版；手機上的網址還是舊版。
4. 把新版存入 GitHub 後，手機網址才會更新。
5. GitHub 也可以當雲端備份，避免 Mac 檔案不小心遺失。

GitHub 專案網址：

`https://github.com/vanisa77-wq/xiaopingguo-calculator-codex`

GitHub Pages 使用網址：

`https://vanisa77-wq.github.io/xiaopingguo-calculator-codex/`

### 從 GitHub 哪裡找到手機網址

1. 打開 GitHub 專案。
2. 點上方的 `Settings`。
3. 左邊選單點 `Pages`。
4. 找到 `Your site is live at`。
5. 下面顯示的網址，就是手機、iPad、Android 可以開的正式網址。

有時候 GitHub 專案首頁右邊的 `About` 區塊，也會顯示同一個網站連結，可以直接點。

### 每次修改後要上傳的檔案

最重要：

- `index.html`：計算機正式版。功能或版面有改，一定要上傳。

如果有修改，也一起上傳：

- `app-icon.png`：手機或電腦圖示。
- `使用說明書.html`：快速說明書。
- `小蘋果計算機_使用說明書.md`：完整文字說明書。

### GitHub 上傳步驟

1. 打開 GitHub 專案。
2. 點「Add file」。
3. 選「Upload files」。
4. 把要更新的檔案拖進去。
5. 如果 GitHub 問是否取代同名檔案，保留新版即可。
6. 下方按「Commit changes」。
7. 等 1 到 3 分鐘，GitHub Pages 網址就會更新。

若手機仍看到舊版，請重新整理網頁，或刪除主畫面圖示後重新加入。

## 十六、升級紀錄

### 2026 年 7 月 21 日

#### 手機按鍵觸控升級

- 縮小觸控裝置上的按鍵間距，減少按鍵之間無反應的空隙。
- 手指落在按鍵間的小縫時，會自動判定並觸發距離最近的按鍵。
- 增加手機架角度較直、手指斜向點按時的操作容錯。
- 滑鼠操作維持原本邏輯，不會套用鄰近按鍵判定。

#### 紙帶與長算式顯示升級

- 移除紙帶長算式的省略號截斷效果。
- 長算式會在加、減、乘、除符號附近自然換行。
- 六位數連續加減的完整算式與答案會保留在紙帶上。
- 完成算式的顯示字級可進一步縮小，減少左側內容被截掉。

#### Mac 安裝說明補充

- 補充 Safari「加入 Dock」反灰的原因與排除方式。
- Mac 安裝必須開啟 GitHub Pages 的 `https://` 正式網址，不能直接使用本機 `file://` 檔案。
- 註明 Safari 網頁 App 需要 macOS Sonoma 14 或更新版本。

### 2026 年 8 月 27 日

#### 新增：台幣⇄日圓匯率換算

- 新增「台幣→日圓」「日圓→台幣」兩顆專屬按鍵，位置在「( )、5%稅、發票、AC、÷」下方。
- 按下後會即時連線查詢匯率（來源：`open.er-api.com`，免金鑰），並在提示列顯示所用匯率與更新時間。
- 匯率會快取 6 小時，重複換算不會每次都重新連線；離線或連線失敗時，會自動使用上次快取的匯率。
- 換算金額來源：直接輸入的數字、剛算出的答案，或先前按過「複製」的本金。
- 換算結果會記入紙帶紀錄，並標示「台幣→日圓」或「日圓→台幣」。
- 日圓→台幣的結果四捨五入至小數點第 2 位；台幣→日圓維持整數日圓。

#### 版面高度調整

- 因新增匯率按鍵導致整台計算機超出畫面高度，需要捲動才能看到「=」鍵，已縮小顯示區、按鍵間距與匯率按鍵本身的高度，讓畫面在不捲動的情況下完整顯示。
- 隨後再加高紙帶顯示區，補回底部多餘的空白區域。
